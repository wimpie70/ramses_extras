docs/FAN_CONFIGURATION_SCHEMA_DRAFT.md
docs/REMOTE_BINDING_IMPLEMENTATION_PLAN.md
docs/ZONES_IMPLEMENTATION_PLAN.md
docs/CONFIGURATION_STRATEGY.md

calibration start: disable automations when we set fan temp to LOW, and afterwards re-enable them

zone ventilation:
on demand, we want the other zones to keep the same ventilation
as would be on low, but, we want extra ventilation on the demand zone.
So, when we set a fan to eg. high, and open up the valves for zone 1 (with the demand),
we want the other zones to close their valves to maintain the same ventilation as would be on low.
Or at least about the same.

What kind of calculation or test do we need to do to determine the valve positions?
We have round 150 mm pipes. The fan now gives:
low: 13 l/s (at 15%)
medium: 41.63 l/s (at 50%)
high: 83 l/s (at 100%)


idea: device simulator: create a device that simulates a device with found real messages.
These messages can be retrieved from _rf docs and stored in a dbase, with options:
- devices (brands, types)
- messages (types, payloads)
- timing (when to send messages)
- enabled (on/off) enable by default, disable when not needed

a test feature can then iterate over these msgs and send them to a selectable sim device
to check responses and find problems. Also can test flooding, etc.
