# Requirements to test the source code
# - last checked/updated: 2026-01-28 (c.f. HA 2026.1.0)
#
# Note: requires ramses_cc v0.52.1 (pre-release) or later

# may need to use this version of ramses_rf...
# -e ~/client/ramses_rf

# for using the library with HA
# - pip list | grep -E 'pytest|voluptuous'

  -r requirements_dev.txt


# used for testing (incl. HA, pytest*, syrupy, voluptuous, etc.)
# - pytest

  pytest >= 9.0.0
  pytest-asyncio >= 1.3.0
  pytest-cov >= 7.0.0
  pytest_homeassistant_custom_component >= 0.13.300

# Python 3.13 compatibility constraints
# Fixes aiodns/pycares compatibility issue in Python 3.13
aiodns==3.5.0
pycares==4.11.0
